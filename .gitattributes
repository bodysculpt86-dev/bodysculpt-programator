* text=auto

# Force LF line endings for shell scripts and container entrypoints to prevent
# CRLF issues that break Linux containers (php-fpm 502 / Railway startup).
*.sh text eol=lf
docker/php-fpm/start-container text eol=lf
docker-entrypoint-railway.sh text eol=lf
